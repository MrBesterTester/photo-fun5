# Default Image

Place your default portrait image here as `default-portrait.jpg`.

The image should be:
- Named exactly: `default-portrait.jpg`
- A valid image file (JPG, PNG, or WebP)
- This will be the image that loads when the app first opens

Users can still upload their own images using the "Replace Image" button or by dragging/dropping a new image.
