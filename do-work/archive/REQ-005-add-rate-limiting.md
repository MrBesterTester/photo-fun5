---
id: REQ-005
title: "Add Server-Side Rate Limiting"
status: completed
claimed_at: 2026-02-19T18:16:00Z
completed_at: 2026-02-19T18:30:00Z
route: B
created_at: 2026-02-19T12:00:00Z
user_request: UR-001
related: [REQ-001, REQ-002, REQ-003, REQ-004, REQ-006, REQ-007]
batch: "SECURITY-step-5"
source_step: "5"
source_doc: "docs/SECURITY-TODO.md"
blueprint_ref: "docs/SECURITY-BLUEPRINT.md"
model_hint: "Opus 4.5"
---

# Add Server-Side Rate Limiting (Step 5)

## What
Set up Upstash Redis and implement persistent server-side rate limiting (sliding window, 10 req / 10 min per IP) in `api/image-edit.ts` to prevent API abuse.

## Checklist
- [ ] 5.1 Create Upstash Redis instance
- [ ] 5.2 Install `@upstash/redis` and `@upstash/ratelimit`
- [ ] 5.3 Add Upstash env vars to Vercel and `.env.local`
- [ ] 5.4 Implement sliding window rate limit (10 req / 10 min per IP)
- [ ] 5.5 Return 429 with `Retry-After` header when exceeded
- [ ] 5.6 Validate: `npx tsc --noEmit`, `npm run build`

## Blueprint Guidance
**Requirement:** R2
**Model:** Opus 4.5 (backend logic)
**Files:** `api/image-edit.ts`, `package.json`, `.env.local`

5.1. Create an Upstash Redis instance via the Upstash console or Vercel integration
5.2. Install: `npm install @upstash/redis @upstash/ratelimit`
5.3. Add `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` to Vercel env vars and `.env.local`
5.4. Implement rate limiting in `api/image-edit.ts`:
  - Use `@upstash/ratelimit` with sliding window: 10 requests per 10 minutes
  - Key on IP from `x-forwarded-for` header (Vercel sets this reliably)
  - Place the check after CAPTCHA verification, before Gemini call
5.5. Return 429 with `Retry-After` header when limit exceeded
5.6. Validate: `npx tsc --noEmit`, `npm run build`

## Context
- **Document set**: SECURITY
- **Specification**: See docs/SECURITY-SPECIFICATION.md for full requirements (R2: Server-Side Rate Limiting)
- **Model recommendation**: Opus 4.5 (advisory — use if your tool supports model selection)

## Dependencies
Depends on Step 4 (REQ-004) for the protection flow ordering (rate limit comes after CAPTCHA). Item 5.1 requires manual Upstash console setup. Step 6 (REQ-006) reuses the same Upstash Redis instance.

---
*Source: docs/SECURITY-TODO.md, Step 5*

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear feature with detailed blueprint guidance. Need to explore current state of api/image-edit.ts (just modified by REQ-004 for reCAPTCHA) to find the right insertion point for rate limiting. Items 5.1 (create Upstash instance) and 5.3 (Vercel env vars) are manual.

**Planning:** Not required

## Plan

**Planning not required** - Route B: Exploration-guided implementation

Rationale: Blueprint provides detailed step-by-step guidance. Just need to find current file state after REQ-004 changes.

*Skipped by work action*

## Exploration

**Key files and current state:**

- **`api/image-edit.ts`**: Protection flow is: parse body → Zod validate → CAPTCHA verify (lines 76-83) → Gemini key check (line 85) → Gemini call. Rate limiting goes between lines 83 and 85 (after CAPTCHA, before Gemini).
- **`package.json`**: Current deps: `@google/genai`, `firebase`, `react`, `react-dom`, `zod`. Missing: `@upstash/redis`, `@upstash/ratelimit`.
- **`.env.local`**: Has `GEMINI_API_KEY`. Missing: `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`.
- **No existing rate limiting or Redis patterns** in codebase — greenfield implementation.
- **IP extraction**: Not yet implemented. Use `request.headers.get('x-forwarded-for')` (Vercel sets this).
- **Existing 429 handling**: Only for Gemini's own quota (lines 213-217), not app-level.
- **Manual items**: 5.1 (create Upstash instance), 5.3 (add env vars to Vercel) require user action.

*Generated by Explore agent*

## Implementation Summary

- **`package.json`**: Installed `@upstash/ratelimit` (^2.0.8) and `@upstash/redis` (^1.36.2)
- **`api/image-edit.ts`**: Added imports for Redis and Ratelimit. Created module-level rate limiter with `slidingWindow(10, "600 s")`. Gracefully handles missing env vars (logs warning, allows request through). Rate limit check inserted after CAPTCHA verification, before Gemini key check. Extracts IP from `x-forwarded-for` header (first IP), fallback to `"unknown"`. Returns 429 with `Retry-After` header and JSON error when exceeded.
- **`.env.local`**: Added placeholder entries for `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` with comments.

**Updated protection flow:** parse body → Zod validate → CAPTCHA verify → Rate limit (IP) → Gemini key check → Gemini call

**Manual items remaining for user:** 5.1 (create Upstash Redis instance), 5.3 (add env vars to Vercel dashboard).

*Completed by work action (Route B)*

## Testing

**Tests run:** `npx tsc --noEmit`, `npm run build`
**Result:** Both pass cleanly. Build output: 215.78 kB JS bundle.

**No unit test framework** in this project — TypeScript type-check + build validation is the primary gate per project conventions.

*Verified by work action*
