---
id: REQ-013
title: Port CI deploy job from samkirk-v3
status: completed
claimed_at: 2026-02-19T18:35:00Z
route: B
completed_at: 2026-02-19T18:45:00Z
created_at: 2026-02-19T20:30:00Z
user_request: UR-004
---

# Port CI Deploy Job from samkirk-v3

## What
Port the `deploy` job from `../samkirk-v3/.github/workflows/ci.yml` into this project's CI workflow, adjusted for photo-fun5's root-level layout (no `working-directory: web` needed).

## Context
samkirk-v3 has a full deploy pipeline that photo-fun5 is missing:
- `deploy` job gated on `build-and-test` + `security-scan` passing (`needs: [build-and-test, security-scan]`)
- Only runs on push to main (`if: github.event_name == 'push' && github.ref == 'refs/heads/main'`)
- Uses `environment: production` protection
- Vercel CLI: `vercel pull` → `vercel build --prod` → `vercel deploy --prebuilt --prod`
- Health check via `vercel inspect` retry loop (5 attempts, 10s apart)
- Requires secrets: `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`

samkirk-v3 also has a `lint` step in `build-and-test` that photo-fun5 lacks. Include if a lint script exists, skip if not.

## Assets
Reference file: `../samkirk-v3/.github/workflows/ci.yml` (lines 88-152)

---
*Source: User asked to carry over CI/CD pipeline from samkirk-v3; confirmed after side-by-side comparison.*

---

## Triage

**Route: B** - Medium

**Reasoning:** Clear port from reference CI file, need to read the source and adapt for root-level layout.

**Planning:** Not required

## Plan

**Planning not required** - Route B: Exploration-guided implementation

Rationale: Clear port task with reference file identified. Just need to read source CI and adapt.

*Skipped by work action*

## Exploration

**Source:** `../samkirk-v3/.github/workflows/ci.yml` lines 88-152

**Deploy job structure (samkirk-v3):**
- `needs: [build-and-test, security-scan]`
- `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`
- `environment: production`
- Steps: checkout → node setup → install vercel CLI → `vercel pull` → `vercel build --prod` → `vercel deploy --prebuilt --prod` → health check (5 retries, 10s apart via `vercel inspect`)
- Secrets: `VERCEL_TOKEN`, `VERCEL_ORG_ID`, `VERCEL_PROJECT_ID`

**Adaptations needed:**
- Remove all `working-directory: web` (photo-fun5 is root-level)
- Remove `cache-dependency-path: web/package-lock.json` (use default)
- No lint script in photo-fun5 → skip lint step

*Generated by Explore agent*

## Implementation Summary

- Added `deploy` job to `.github/workflows/ci.yml`
  - Gated on `build-and-test` + `security-scan` passing
  - Only runs on push to main
  - Uses `environment: production` protection
  - Full Vercel CLI pipeline: pull → build → deploy → health check
  - Health check: 5 retries with `vercel inspect`, 10s apart
  - Requires secrets: VERCEL_TOKEN, VERCEL_ORG_ID, VERCEL_PROJECT_ID
- Adapted from samkirk-v3: removed `working-directory: web` and `cache-dependency-path`
- Skipped lint step (no lint script in package.json)

*Completed by work action (Route B)*

## Testing

**Tests run:** `npm test` (static security tests), `npx tsc --noEmit`, `npm run build`, YAML validation
**Result:** All passing — 11 tests pass, build succeeds, YAML valid

*Verified by work action*
